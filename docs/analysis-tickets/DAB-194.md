# DAB-194: Dead code — 2,027 removable lines, verified twice on independent copies

> **STATUS: DONE.** Landed on `dev` as `9894bcc` "Remove verified dead code from the source tree"
> (code, config fields, imports) and `4baa29c` "Drop five unused runtime dependencies".
> **Predicted vs delivered:** the ticket forecast **-2,027 lines, 23,973 -> 21,946** Python LOC and
> **87** unused import bindings; `9894bcc`'s own diffstat is **25 files, 18 insertions, 2,111
> deletions** and its message reports **96** unused import bindings. Re-measured against the current
> tree (`wc -l` over every `.py` in `main.py`, `src`, `scripts`, `tests`): **23,973 -> 21,895**, a
> net **-2,078 (-8.7%)** — **51 lines below the predicted 21,946**, i.e. the applied list ran
> slightly wider than the estimate. The rest landed on the nose: `.py` files **67 -> 66**,
> `requirements.in` **16 -> 11** declared packages, `constraints.txt` **57 -> 51** pins. Verified
> here: `compileall` clean, `Ran 125 tests ... OK`. The one criterion not met is the commit
> sequencing — see Acceptance criteria.

- Type: IMPROVEMENT
- Severity / Payoff: S4 per constituent finding. **-2,027 lines (-8.5%)**, 23,973 -> 21,946 Python
  LOC, `compileall` clean and 125/125 tests green on two independent `/tmp` copies.
- Effort: S
- Priority score: 3.0 — impact Low (1) x frequency Common (3) / effort S (1)
- Confidence: **VERIFIED** — the list is empirical, not speculative: it was applied and the suite
  re-run
- Merged IDs: `C6-01` … `C6-11`, `C6-13`, `C6-16`, `C6-17`, `C6-23`; the commit sequencing is
  `I1-50`, the `help_system.py` half is `I6-07`
- Blocks / Blocked by: **nothing — the `DAB-203` edge was refuted.** This ticket originally claimed
  a naive scan deletes the live `/image-queue` command and no test fails. That is false:
  `tests/test_command_registration.py:239-240` (the `[5:10]` slice) and `:256` both pin the command
  in the image-enabled tree, and deleting it gives `FAILED (failures=1)`. The claim came from
  grepping `image_queue` while the test spells it `image-queue`. See
  [`../ANALYSIS_CORRECTIONS.md`](../ANALYSIS_CORRECTIONS.md) item 1. The deny-filter *discipline*
  still matters — three live gateway handlers can be deleted with the suite green — but it is a
  review rule, not a dependency on `DAB-203`.

## Context

Three independent dead-code passes were run — an AST import graph, an AST definition inventory with
word-boundary reference counting, and a dynamic-dispatch backstop over every string literal
appearing in a `getattr`/`hasattr`/`setattr` call in `src/` and `tests/`. The resulting list was then
**applied to a `/tmp` copy and the suite re-run**.

The real value is not the line count. It deletes `help_system.py`, which is the only consumer of six
`UserExperienceService` methods and the source of 8 of the 13 dangling `/help` promises.

## Evidence

### The inventory

**Whole module (1):** `src/services/help_system.py` — **636 lines**, zero importers anywhere. It is
the only production module (excluding entry points and package `__init__.py`s) with zero importers,
and it is absent from `src/services/__init__.py:8-32`. `help_system.py:17` is the **only** consumer
of `UserExperienceService.create_status_embed` and the `Status` enum outside
`user_experience_service.py` itself — deleting it is what makes the next item fully collapse.

**Whole classes (2):**
`src/utils/logging_config.py:505-618` — `class ImageProcessingLogger` (114 lines, 0 refs);
`src/utils/logging_config.py:621-709` — `class MessageFormattingLogger` (89 lines, 0 refs).

**Methods / functions (50):**

```
src/bot/discord_bot.py (11 dead wrappers): :701 _get_live_context_buffer,
  :719 _run_live_channel_worker, :723-742 _append_live_context_entry,
  :1202-1217 _create_image_context_entry, :1311-1316 _update_progress_message,
  :1318-1320 _add_error_reaction, :1322-1324 _get_user_friendly_error_message,
  :1406-1409 _clean_split_part_for_embed, :1429-1435 _send_split_response,
  :1437-1443 _send_simple_split_response, :1445-1450 _send_grounding_sources

src/bot/response_generation.py (3, remove with the above): :55-66 update_progress_message,
  :68-74 add_error_reaction, :76-95 get_user_friendly_error_message

src/services/user_experience_service.py (8): :122 show_typing_indicator,
  :176 stop_typing_indicator, :194 create_status_embed, :266 add_reaction_feedback,
  :321 send_progress_update, :376 send_processing_notification,
  :423 send_completion_notification, :472 create_help_embed

src/utils/error_manager.py (6): :337 get_technical_message, :481 handle_image_processing_error,
  :535 handle_message_formatting_error, :577 suggest_error_resolution,
  :629 log_user_action_error, :674 attempt_error_recovery

src/services/gemini_client.py (5): :167 get_prompt_mode, :173 set_prompt_mode,
  :192 get_force_search, :290 get_timeout_for_current_model, :346 set_model_by_complexity

src/utils/logging_config.py (3): :251 PerformanceLogger.log_image_processing,
  :296 ...log_message_splitting, :343 ...log_user_action

Singles (9): src/config.py:236 BotConfig.get_safety_threshold,
  src/services/channel_settings_service.py:112 list_personalities,
  src/services/context_collector.py:310 collect_full_context,
  src/services/image_processing_service.py:260 cancel_job  (needs-care: discards a written but
    unreachable feature),
  src/services/message_index_service.py:1400 get_messages_by_ids_async,
  src/services/message_splitter.py:490 split_long_message,
  src/utils/image_utils.py:127 get_supported_formats,
  src/utils/image_utils.py:137 get_supported_extensions,
  src/utils/markdown_utils.py:322 MarkdownParser.get_markdown_context
```

Line counts per group: 11 `DiscordBot` wrappers = 78; 3 `ResponseGenerationCoordinator` statics =
39; 8 `UserExperienceService` methods = 375; 6 `ErrorManager` methods = 253; 5 `GeminiClient`
methods = 56; 3 `PerformanceLogger` methods + 2 logger classes = 335; 9 singles = 153.
`PerformanceLogger` itself stays live (`discord_bot.py:326`, `gemini_client.py:55`).

**Config fields (4) — delete all three sites per field:**

| Field | `src/config.py` | `src/config_helpers.py` | `config.yaml` |
|---|---|---|---|
| `api_timeout_buffer` | `:78` | `:244-246` | `:61` |
| `edit_detection_max_output_tokens` | `:120` | `:364-366` | `:138` |
| `system_prompt_thinking_addon` | `:167` | `:435-437` | `:206` (`system_prompts.thinking_mode_addon`) |
| `progress_update_threshold` | `:180` | `:467-469` | `:354` |

**Enum members (3, plus their 6 dict entries):** `src/utils/error_manager.py:37 MISSING_TOKEN`
(+ `:113`, `:152`), `:38 INVALID_TOKEN` (+ `:114`, `:153`), `:42 MESSAGE_HISTORY_ERROR`
(+ `:118`, `:155`).

**Constants / attributes (2):** `src/services/content_renderer.py:90-93` `RAW_LATEX_RE` (keep
`RAW_LATEX_COMMANDS:83-89`); `src/bot/discord_bot.py:378` the `self._message_index_service` alias.

**Unused imports (87 bindings, 14 files):** `commands.py` 13, `configuration.py` 15, `general.py` 13,
`personalization.py` 11, `reports_usage.py` 11, `research.py` 10, `discord_bot.py` 4,
`enhanced_command_handler.py` 1, `context_collector.py` 1, `image_processing_service.py` 2,
`message_splitter.py` 1, `image_utils.py` 3, plus `help_system.py` (2, deleted with the module) and
one each in `user_experience_service.py` / `error_manager.py` that only become unused *after* the
method deletions — **run the detector again post-cleanup.** Derivation: 102 flagged, minus 11
`__future__`, minus 5 load-bearing re-exports.

**Dependencies (5 lines from `requirements.in`):** `requests>=2.31.0` (L12), `markdown>=3.5.0`
(L17), `beautifulsoup4>=4.12.0` (L18), `pdf2image>=1.16.0` (L9), `colorlog>=6.7.0` (L22). 16 -> 11
declared packages; `constraints.txt` 57 -> 51 pinned lines. `requests` removal is **0 lines** off
`constraints.txt` — it stays as a hard runtime requirement of `google-genai` 2.11.0.
`jinja2` **stays**: imported in 6 files, used in exactly 1 (`research.py:86`); the other 5 imports
are in the unused-import list.

### The verified result

```
$ python -m compileall -q main.py src scripts tests
COMPILE_OK

$ python -m unittest discover -s tests -p "test_*.py"
Ran 125 tests in 1.028s
OK

Python LOC: 23,973 -> 21,946   (-2,027 lines, -8.5%)
```

The unused-import cleanup **alone**, on an otherwise pristine copy, is also `compileall` clean and
125/125. The two halves can ship as independent commits.

A second, narrower pass computed **-1,755** for a code-only scope (imports and config fields
deferred). Both are correct for their scope. Publish **-2,027** as the total and **-1,755** as the
code-only subtotal.

## Reproduction

Apply the list above to a `/tmp` copy, then:

```
python -m compileall -q main.py src scripts tests
python -m unittest discover -s tests -p "test_*.py"
```

## Acceptance criteria

- [x] `python -m unittest discover -s tests -p "test_*.py"` stays at **125 tests, OK**.
- [x] `python -m compileall -q main.py src scripts tests` stays clean.
- [x] **No conditionally-registered command is deleted.** `/edit-image` and `/image-queue` still
      register when `image_processing_service` is present — `general.py:113` and `:250`, both still
      under the `hasattr(bot, 'image_processing_service')` guard.
- [x] The deny-filter below was applied before any deletion. No `on_*` handler and no command
      decorator was touched: `git diff c83f740 HEAD -- '*.py' | grep '^-.*def on_'` is empty.
- [x] `constraints.txt` is **regenerated** with `pip freeze --exclude pip` from a clean 3.12
      environment, never hand-edited — `4baa29c`'s message records the regeneration from a clean
      3.12.13 virtualenv; 57 -> 51 pins.
- [ ] Each config-field deletion is one commit touching all three sites. **Not met.** All four
      fields were removed in the single commit `9894bcc`, together with every other deletion; the
      planned 8-commit review order became two commits. All three sites *per field* were removed,
      so the sequencing hazard below never fired, but the per-field commit boundary was not kept.

## Files to touch

`src/services/help_system.py` (delete), `src/services/user_experience_service.py`,
`src/utils/logging_config.py`, `src/utils/error_manager.py`, `src/bot/discord_bot.py`,
`src/services/gemini_client.py`, `src/services/image_processing_service.py`,
`src/bot/response_generation.py`, `src/utils/markdown_utils.py`,
`src/services/context_collector.py`, `src/utils/image_utils.py`,
`src/services/message_splitter.py`, `src/config.py`, `src/config_helpers.py`, `config.yaml`,
`src/services/channel_settings_service.py`, `src/services/message_index_service.py`,
`src/services/content_renderer.py`, `requirements.in`, `constraints.txt` (regenerate), plus the 14
files in the unused-import list.

Land the list **unmodified**, in an 8-commit review order, with the deny-filter as commit zero.

## Test to add

The acceptance is the **existing** suite, unchanged. Beyond that, add the guard that makes the
cleanup safe to repeat:

`tests/test_command_registration.py` (as part of `DAB-203`):

```
test_conditional_commands_register_when_their_service_is_present:
    bot = await self._register(image_processing_service=object())
    assert "image-queue" in command names
    assert "edit-image" in command names
```

If a future cleanup deletes a conditionally-registered command, that test fails. Today nothing does.

## Risks and notes

### The mandatory deny-filter — non-negotiable

**Do not run a generic dead-code scan against this repo without it.** `image_queue` is **not** in
the SAFE list, but it is exactly the shape of thing a naive re-derivation deletes:

```
src/bot/command_modules/general.py:124   if hasattr(bot, 'image_processing_service') and bot.image_processing_service:
src/bot/command_modules/general.py:263       @bot.tree.command(name="image-queue", ...)
src/bot/command_modules/general.py:264       async def image_queue(interaction: discord.Interaction):

$ grep -rn "image_queue" --include=*.py --include=*.md --include=*.yaml .   # excluding .venv
./src/bot/command_modules/general.py:264:        async def image_queue(interaction: discord.Interaction):
```

**Exactly one reference in the entire repo — its own definition.** Invisible to reference counting
*and* to the test suite, because `test_command_registration._register()` passes
`image_processing_service=None` and then asserts `len(bot.tree.get_commands()) == 22`, pinning the
**degraded** tree. That is `DAB-203`.

The three hazard classes the filter must deny:

1. Any `^on_[a-z_]+$` method on a `discord.Client` / `discord.ui.*` subclass — 254 lines of live
   dispatch (`on_message` 130, `on_ready` 86, `on_error` 14, `on_raw_message_delete` +
   `on_raw_bulk_message_delete` 10, `on_disconnect` + `on_resumed` 6,
   `SplitResponsePaginatorView.on_timeout` 8).
2. **Any function defined inside a conditional registration block** — `image_queue` (52 lines) and
   `edit_image`, both under `general.py:124`.
3. Any `@discord.ui.button` callback.

### Other things that look dead and are not

`SplitResponsePaginatorView.on_timeout` (`response_delivery.py:91`); the
`SplitResponsePaginatorView` import at `discord_bot.py:46` (pinned by
`tests/test_discord_orchestration.py:8,354`); `_format_report_status`, `_format_timedelta`,
`_get_model_description`, `_truncate_text` at `src/bot/commands.py:26` (pinned by
`tests/test_command_registration.py:15-21` and `test_public_command_helpers_remain_importable`
at `:386`); `BotConfig.rag_gating_enabled` (read via `getattr` at
`hybrid_context_retriever.py:514`); `BotConfig.min_token_length_discord/gemini` (read at
`src/config.py:200,204`); `_get_live_channel_lock`, `_process_live_messages`,
`_build_live_user_prompt` (test-only, `tests/test_bug_regressions.py:188-244`).

**Do not touch** `src/bot/commands.py:26` (4 helpers) or `src/bot/discord_bot.py:46`
(`SplitResponsePaginatorView`) — they are pinned re-exports. Add explanatory comments instead.

### Sequencing hazard caught during verification

Deleting a `BotConfig` field *without* its `config_helpers.py` parser entry breaks 3 tests with
`TypeError: BotConfig.__init__() got an unexpected keyword argument 'api_timeout_buffer'`
(`src/config.py:185` in `from_yaml`). That is the concrete cost of the four-places rule. One commit
per field, all three sites.

### Coordination

- **`api_timeout_buffer` was deleted in `9894bcc` without that question being settled.** All three
  sites are gone — the `config.yaml` key, the `BotConfig` field and the `config_helpers.py` parser —
  and `grep -rn api_timeout_buffer --include=*.py --include=*.yaml .` outside `.venv` now returns
  nothing. `DAB-042` cannot wire it in, because there is nothing left to wire: a whole-sequence
  budget has to introduce a **new** key through all four places — the `config.yaml` key, a parser in
  `src/config_helpers.py`, a field on `BotConfig` in `src/config.py`, and a rule in the matching
  `_validate_*` helper.
- **`set_model_by_complexity` is in the delete list**, and it is also the root of `DAB-045` (zero
  callers -> `_current_complexity_level` frozen at `'low'`, four reporting surfaces wrong). Deleting
  the method does not fix the frozen state or the wrong reports; it just removes the misleading
  affordance. Read `DAB-045` first.
- **`cancel_job` (`image_processing_service.py:260`) is the one "needs-care" entry** — it discards a
  written but unreachable feature. `tests/test_bug_regressions.py:253,289` exercise
  `ImageProcessingService.stop()` and worker cancellation, **never** `cancel_job`.
- Explicitly **not** in this cleanup: the except-clause reorder (behaviour change);
  `message_retrieval_events` retention (a product decision, `DAB-086`); `config.yaml.example`;
  the `.gitignore` `data/*` fix (`DAB-157`); the `grok-prompts` gitlink (`DAB-180`); the `/help`
  promises (`DAB-195`); the `/features` copy (`DAB-196`); the six `datetime.utcnow()` call sites
  (`DAB-110`); retiring `UserExperienceService` entirely.
- `tests/test_config.py` does not name the four dead fields, but three tests fail if a field is
  deleted without its parser entry.
